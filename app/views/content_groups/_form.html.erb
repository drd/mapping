<%= form_for(@content_group) do |f| %>
  <% if @content_group.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@content_group.errors.count, "error") %> prohibited this content_group from being saved:</h2>

      <ul>
      <% @content_group.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :offering_id %><br />
    <%= f.number_field :offering_id %>
  </div>
  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
